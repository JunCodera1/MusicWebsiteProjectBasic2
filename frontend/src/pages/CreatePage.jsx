import React from 'react';

const CreatePage = () => {
  const style = {
    padding: '20px',
    textAlign: 'center',
    fontSize: '24px',
    color: '#333'
  };

  return (
    <div style={style}>CreatePage</div>
  );
}

export default CreatePage;
